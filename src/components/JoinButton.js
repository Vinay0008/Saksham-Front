import { useState } from "react"; import { Box, Button } from "@mui/material"; import ArrowForwardIcon from "@mui/icons-material/ArrowForward"; import JoinNowDialog from "./JoinNowDialog"; export default function JoinButton() { const [open, setOpen] = useState(false); return ( <> <Box sx={{ display: "flex", justifyContent: "center", alignItems: "center", py: 2, background: "transparent", }} > <Button variant="contained" endIcon={<ArrowForwardIcon />} onClick={() => setOpen(true)} sx={{ backgroundColor: "#f57c00", color: "#fff", fontWeight: 700, borderRadius: 5, fontSize: 18, textTransform: "none", "&:hover": { backgroundColor: "#e86b00" }, }} > Join Now!! </Button> </Box> <JoinNowDialog open={open} onClose={() => setOpen(false)} /> </> ); }